# Loan Default Prediction API
loan default prediction using structured financial data (give me some credit)

## Overview

A Python-based **FastAPI ML API** that predicts whether a user will default on a loan in 2 years.  
Includes **user authentication**, **database storage** (SQLAlchemy + Alembic), and a pre-trained **XGBoost model**.
